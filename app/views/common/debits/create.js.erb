<% if @debit.errors.any? %>
  $('#common_modal #common-errors').remove();
  $('#common_modal .modal-body').prepend("<%= j( render partial: 'shared/common_errors', locals: { model: @debit } ) %>");
<% else %>
  $('#common_modal').modal('hide');
  location.href='/debit'
<% end %>